---
name: jev-model-router
description: Pick a model from the catalog with model_route.
---

# JEV Model Router

Use the `model_route` tool from the `jev-model` MCP server.

Required: task.

Never pass API keys in the tool arguments.
